{
  properties: [
    {
      name: "count"
      description: "The number of jars to create"
      type: "int"
      required: true
    }
    {
      name: "outdir"
      description: "The directory in which to generate"
      type: "string"
      required: true
    }
  ]
}